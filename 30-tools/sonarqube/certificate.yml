apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: sonar-tls
spec:
  issuerRef:
    group: cert-manager.io
    kind: ClusterIssuer
    name: lets-encrypt
  secretName: sonar-tls
  dnsNames:
    - sonar.check-consulting.net
  usages:
    - digital signature
    - key encipherment