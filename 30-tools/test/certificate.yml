apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: test-tls
spec:
  issuerRef:
    group: cert-manager.io
    kind: ClusterIssuer
    name: lets-encrypt
  secretName: test-tls
  dnsNames:
    - test.check-consulting.net
  usages:
    - digital signature
    - key encipherment